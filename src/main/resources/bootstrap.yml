server:
  port: 15001
spring:
  profiles:
    active: local
    include: eureka
  output.ansi.enabled: 'ALWAYS'
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:15000